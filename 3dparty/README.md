# HuxerUI Third-Party Dependencies

All vendored third-party dependencies live under `3dparty`.
Each dependency directory records its upstream version, source files, license, and local compatibility changes in `METADATA`.
